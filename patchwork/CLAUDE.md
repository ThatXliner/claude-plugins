# patchwork plugin

AST-native sed for Claude Code. Provides the `patchwork` skill for structural code transformations.

## Installation

```bash
cargo install patchwork-cli
```

## Usage

Use `patchwork` CLI for AST-aware find/replace/delete/insert instead of sed/regex when working with code.
